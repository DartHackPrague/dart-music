class CanvasCircle {
  var position;
  var color;
  var opacity;

  CanvasCircle(Position position, RgbColor color) {
    this.position = position;
    this.color = color;
    this.opacity = 1;
  }
}
